services:
  - type: web
    name: dark-horse-api-v23
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main_v2:app --host 0.0.0.0 --port $PORT
    plan: free
